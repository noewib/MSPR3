apiVersion: v1
kind: Service
metadata:
  name: edf-consumption-predictor-service
  namespace: edf-rte-staging
  labels:
    app: edf-consumption-predictor
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: edf-consumption-predictor
    environment: staging
